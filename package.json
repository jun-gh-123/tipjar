{
  "name": "tipjar",
  "version": "1.0.0",
  "description": "",
  "homepage": "https://github.com/jun-gh-123/tipjar#readme",
  "bugs": {
    "url": "https://github.com/jun-gh-123/tipjar/issues"
  },
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.ts",
  "scripts": {
    "start": "esbuild index.ts --outfile=www/app.js --allow-overwrite --minify --bundle --watch --servedir=www",
    "build": "esbuild index.ts --outfile=www/app.js --allow-overwrite --minify --bundle"
  },
  "dependencies": {
    "@mysten/dapp-kit-core": "^1.6.22",
    "@mysten/sui": "^2.28.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
